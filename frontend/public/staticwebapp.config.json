{
  "navigationFallback": {
    "rewrite": "/index.html"
  },
  "routeConfiguration": {
    "routes": [
      {
        "route": "/api/*",
        "allowedRoles": ["authenticated"]
      }
    ]
  }
}